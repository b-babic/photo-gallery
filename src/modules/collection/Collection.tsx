import { FunctionComponent, ReactElement } from 'react';

interface Props {}

export const CollectionPage: FunctionComponent<Props> = (): ReactElement => {
  return (
    <main role="main">
      <h2>Collection page here</h2>
    </main>
  );
};
